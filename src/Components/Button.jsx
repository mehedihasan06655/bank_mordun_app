
const Button = ({styles}) => {
  return (
    <button type="button" className={`${styles} py-4 px-6 font-poppins text-[18px] font-medium text-primary bg-blue-gradient rounded-[10px]`}>
      Get Started
    </button>
  )
}

export default Button